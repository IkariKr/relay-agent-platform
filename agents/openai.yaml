interface:
  display_name: "Codex Delegate Claude"
  short_description: "Delegate coding work to Claude, then review"
  default_prompt: "Use $codex-delegate-claude to delegate this coding task to Claude Code, review the diff, and commit only verified changes."
