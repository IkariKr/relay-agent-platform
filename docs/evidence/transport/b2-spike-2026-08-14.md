# B2 Transport Spike — Evidence & Decision Note

> 记录日期：2026-08-14  
> 类型：evidence + ADR / decision note（roadmap §7 Phase B2 与 §8.2 退路分支）  
> 关联证据：[A2 capability probe](../codex-capability/2026-08-14-codex-0.142.5-win32.json)

## 1. B2 目标（roadmap Phase B2）

用最小 deterministic assignment 验证 Codex 原生 plaintext transport：native child 创建、fork 隔离语义、child 收到完整 marker、原生 wait/callback，并生成本次 transport evidence。全部通过则不实现 Hook；是否进入 B3 必须由 evidence + ADR 记录。

## 2. 当前环境事实（证据）

| 项 | 值 | 来源 |
|---|---|---|
| codex cli | 0.142.5（`C:\Users\ckr2577\AppData\Roaming\npm\codex.ps1`） | A2 evidence `host_identity` |
| `[agents]` 段 | **缺失**（`agents=False`） | A2 evidence `custom_agent_discovery` |
| `[model_providers]` 段 | 存在（`model_providers=True`） | A2 evidence `custom_provider_load` |
| custom_agent_spawn / fork_isolation / native_wait_callback / native_cancel / plaintext_initial_message / plaintext_followup_message | **全部 unknown（需活宿主）** | A2 evidence |
| DeepSeek pack 注册格式 | 未验证（`[agents]` 段缺失，config.toml role 注册语法未确认） | B1 install_contract.note |

## 3. 为什么 spike 现在不能执行

1. **付费红线**：B2 最小 spike 需要真实调用第三方 provider（DeepSeek 计费）。roadmap §10 红线："paid smoke、写 worker、敏感数据外发必须显式授权"。本记录日期未获用户对该 spike 的显式授权，因此不得执行。
2. **宿主能力未知**：A2 evidence 显示 spawn/isolation/callback/plaintext 全部为 unknown，属于"需活宿主"行为。在没有能力证据前运行 spike，违反"行为证据优先"原则。
3. **pack 注册格式未验证**：`[agents]` 段缺失，B1 只做了 pack-owned 文件暂存；config.toml 的 role 注册语法需先由配置解析验证，否则 spike 无法以正确形态创建 child。

## 4. 决策（roadmap §8.2 退路分支）

- **B2 延期**（defer）：不在本环境未授权状态下执行付费 spike。
- **B3 不进入**：没有证据证明"native spawn/lifecycle 可用、仅 task delivery 被阻断"，不满足进入 Hook spike 的前置条件。
- **B4 延期**：paid smoke 属显式授权操作，未授权前不执行；README 不宣称 DeepSeek 为 supported native child（claim gate 保持）。
- **不伪造 native**：不用 CLI/SDK/MCP/第二 Codex 冒充 native child（roadmap §1 条件 5、§8.2 第 4 条）。
- 当前交付状态 = **A0-A2 + B1 完整**；external-cli 路线继续可用；Worker Runtime Registry、capability probe、DeepSeek pack、dispatch（Phase C）均可独立演进。

## 5. 重开条件

当以下条件满足时，用新 evidence 重开 B2/B3，无需推翻 Worker Registry：

1. 用户显式授权付费 spike（提供/确认 DEEPSEEK_API_KEY 与计费意愿）；
2. `[agents]` role 注册格式经配置解析验证（capability/config probe 通过）；
3. 重新运行 capability probe 生成新的 host evidence。

之后按 roadmap Phase B2 → B3（仅当 evidence 指示）→ B4 顺序推进。
