interface:
  display_name: "Relay OpenCode"
  short_description: "Delegate coding work to OpenCode, then review"
  default_prompt: "Use $relay-opencode to delegate this coding task to OpenCode, review the diff, and commit only verified changes."