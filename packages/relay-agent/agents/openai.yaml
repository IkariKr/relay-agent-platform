interface:
  display_name: "Relay"
  short_description: "Route coding work to Claude or OpenCode, then review"
  default_prompt: "Use $relay-agent to choose the right backend, delegate this coding task, review the diff, and commit only verified changes."